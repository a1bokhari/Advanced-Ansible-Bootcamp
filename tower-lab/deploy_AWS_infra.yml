---
- name: Playbook to deploy infrastructure VMs in AWS
  hosts: localhost
  gather_facts: no
  vars_files:
    - vars/vars.yml
  tasks:
    - name: Create "bin" directory
      file:
        path: "/tmp/bin"
        state: directory
        mode: 0755

    - name: Get "jq" binary
      get_url:
        url: http://www.opentlc.com/download/ansible_bootcamp/scripts/jq-linux64
        dest: "/tmp/bin/jq"
        mode: 0755

    - name: Get shell scripts
      get_url:
        url: "http://www.opentlc.com/download/ansible_bootcamp/scripts/{{ item }}"
        dest: "/tmp/{{ item }}"
        mode: 0755
      with_items:
        - common.sh
        - order_svc.sh

    - name: Create credentials file
      template:
        src: templates/credential.rc.j2
        dest: /tmp/credential.rc

    - name: Create the AWS service
      shell: "source /tmp/credential.rc ; /tmp/order_svc.sh -c 'OPENTLC Automation' -i 'Three Tier Application' -t 1 -y"

