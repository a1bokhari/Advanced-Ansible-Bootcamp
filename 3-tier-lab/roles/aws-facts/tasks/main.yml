---
# tasks file for roles/aws-facts
- name: Fetch Instance Info
  ec2_instance_facts:
    region: us-east-1
    filters:
      "tag:instance_filter": "three-tier-app-abokhari*"
  register: result

- name: print
  debug:
    var: result
